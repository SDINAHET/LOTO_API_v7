# # Profile de test minimal
# spring.profiles.active=test

# # Evite les effets de bord pendant les tests (schedulers/scraping)
# spring.task.scheduling.enabled=false
# spring.main.banner-mode=off
# logging.level.root=WARN

# ========= PROFIL =========
# spring.profiles.active=test

# ========= POSTGRES =========
# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.url=jdbc:postgresql://localhost:5432/lotodb_test
spring.datasource.username=postgrestest
spring.datasource.password=postgrestest

# spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# ========= MONGODB =========
# spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}
spring.data.mongodb.uri=mongodb://localhost:27017/lotodb_test

# ========= MIGRATIONS =========
spring.flyway.enabled=false
spring.liquibase.enabled=false

# ========= SCHEDULERS =========
spring.task.scheduling.enabled=false

# ========= SPRING =========
spring.main.banner-mode=off
logging.level.root=WARN

# ========= SECURITY (JWT) =========
# on garde Spring Security actif !
spring.security.filter.dispatcher-types=REQUEST,ERROR

# spring:
#   task:
#     scheduling:
#       enabled: false


# # 👇 AJOUT IMPORTANT
# app:
#   scraping:
#     enabled: false
#   prediction:
#     enabled: false


# # ⚡ énorme gain
# spring.main.lazy-initialization=true

# # déjà OK chez toi mais je complète
# spring.task.scheduling.enabled=false
# spring.jpa.open-in-view=false





# # ========= PROFIL =========
# # spring.profiles.active=test

# # ========= DATABASE (H2 IN-MEMORY POUR TESTS) =========
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false
# spring.jpa.open-in-view=false

# # ========= MONGODB =========
# spring.data.mongodb.uri=mongodb://localhost:27017/lotodb_test

# # ========= MIGRATIONS =========
# spring.flyway.enabled=false
# spring.liquibase.enabled=false

# # ========= SCHEDULERS =========
# spring.task.scheduling.enabled=false

# # ========= SPRING =========
# spring.main.banner-mode=off
# logging.level.root=WARN

# # ========= SECURITY (JWT) =========
# spring.security.filter.dispatcher-types=REQUEST,ERROR

# # ========= APP =========
# app.scraping.enabled=false
# app.prediction.enabled=false




# # ========= PROFIL =========
# spring.profiles.active=test

# # ========= POSTGRES =========
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/lotodb_test}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgrestest}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgrestest}

# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false

# # ========= MONGODB =========
# spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/lotodb_test}

# # ========= MIGRATIONS =========
# spring.flyway.enabled=false
# spring.liquibase.enabled=false

# # ========= SCHEDULERS =========
# spring.task.scheduling.enabled=false

# # ========= SPRING =========
# spring.main.banner-mode=off
# spring.main.lazy-initialization=true
# logging.level.root=WARN

# # ========= SECURITY =========
# spring.security.filter.dispatcher-types=REQUEST,ERROR
